import { useParams } from "react-router-dom";

export const ItemDetal = () => {
  const { id } = useParams();
  //TODO: use this id to search by item console.log(id);
  return <div>ItemDetal</div>;
};
